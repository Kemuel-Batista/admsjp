export enum EventType {
  REMOTO = 0,
  PRESENCIAL = 10,
  HIBRIDO = 20,
}

export enum EventStatus {
  INACTIVE = 0,
  ACTIVE = 1,
}

export enum EventVisible {
  INVISIBLE = 0,
  VISIBLE = 1,
}

export enum EventPixType {
  CPF = 1,
  CNPJ = 2,
  EMAIL = 3,
  PHONE = 4,
  CHAVE = 5,
}
